数据库初始化执行 migrations 脚本。
Web后端配置 .env 并启动服务，推荐 Nginx+PHP-FPM 或 PHP 内置服务器。
设置 API_TOKEN 与 HMAC_SECRET，启用 HTTPS。
Android 安装后授予 READ_SMS 与悬浮窗权限，开启前台服务与白名单保活。
设备监控页面访问 webserver/public/admin.php 查看状态与短信记录。
